import usePageTitle from "../../Hooks/usePageTitle";

function About() {
    usePageTitle('About | My App');

    return (
        <div>
            <h1>About Page</h1>
        </div>
    );
}

export default About; 