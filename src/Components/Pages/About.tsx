import usePageTitle from "../Layout/usePageTitle";

function About() {
    usePageTitle('About | My App');

    return (
        <div>
            <h1>About Page</h1>
        </div>
    );
}

export default About; 